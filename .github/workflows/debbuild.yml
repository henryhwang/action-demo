name: Build debian package
on:
  workflow_dispatch
jobs:
  do-in-container:
    runs-on: ubuntu-latest
    container: debian:sid
    steps:
      - name: demo
        run: |
          apt update
          apt install lsb-release
          lsb_release
